{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capability for The Last IceyPop. The game runs entirely in the webview; no native APIs are required.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
